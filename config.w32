// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("veriexec", "for veriexec support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("veriexec", "enable veriexec support", "no");

if (PHP_VERIEXEC != "no") {
	EXTENSION("veriexec", "veriexec.c");
}

